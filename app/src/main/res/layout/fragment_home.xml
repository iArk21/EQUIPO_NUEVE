<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_wood_pine"
    android:fitsSystemWindows="true"
    tools:context=".ui.home.HomeFragment">
|
    <!-- HU 3.0: Toolbar personalizada con bordes redondeados y margen superior -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/toolbar_container"
        android:name="com.example.pico_botella.ui.toolbar.ToolbarFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout="@layout/fragment_toolbar" />

    <!-- HU 2.0: Botella en el centro -->
    <ImageView
        android:id="@+id/iv_bottle"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/ic_bottle_cartoon"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- HU 2.0: Contador regresivo sobre la botella -->
    <TextView
        android:id="@+id/tv_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@android:color/white"
        android:textSize="80sp"
        android:textStyle="bold"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/iv_bottle"
        app:layout_constraintEnd_toEndOf="@id/iv_bottle"
        app:layout_constraintStart_toStartOf="@id/iv_bottle"
        app:layout_constraintTop_toTopOf="@id/iv_bottle" />

    <!-- HU 2.0: Botón Presióname -->
    <Button
        android:id="@+id/btn_press_me"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:backgroundTint="#FF9800"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:text="Presióname"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>